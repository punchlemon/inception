# Ignore secrets and credentials
secrets/*.txt
secrets/*.crt
secrets/*.key
secrets/*.pem

# Allow example templates
!secrets/*.example

# Ignore .env files with real credentials
srcs/.env

# Ignore host data volumes
/home/retanaka/data/
